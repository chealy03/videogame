import 'package:flutter/material.dart';

class BrickBreakerGamePage extends StatelessWidget {
  const BrickBreakerGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}